const TASKS=[
{id:'app1',type:'apps',icon:'smartphone',title:'Download & Open',description:'Install and open the demo app.',reward:500,time:'2 min'},
{id:'app2',type:'apps',icon:'rocket',title:'Explore Launch',description:'Explore a fictional product demo.',reward:600,time:'3 min'},
{id:'app3',type:'apps',icon:'shopping-bag',title:'Try Shoply',description:'Browse a sample shopping experience.',reward:450,time:'2 min'},
{id:'survey1',type:'surveys',icon:'clipboard-list',title:'Quick Survey',description:'Share your mobile preferences.',reward:750,time:'5 min',survey:true},
{id:'survey2',type:'surveys',icon:'message-square-more',title:'Product Pulse',description:'Tell us what you think of this concept.',reward:550,time:'4 min',survey:true},
{id:'survey3',type:'surveys',icon:'bar-chart-3',title:'Lifestyle Check-in',description:'Answer a few light questions.',reward:400,time:'3 min',survey:true},
{id:'game1',type:'games',icon:'gamepad-2',title:'Tap Challenge',description:'Tap fast to complete the challenge.',reward:200,time:'1 min',game:true},
{id:'game2',type:'games',icon:'target',title:'Focus Target',description:'A second fictional game challenge.',reward:300,time:'2 min',game:true}];
const DEMO={balance:12450,completedTasks:[],transactions:[{id:'t1',icon:'smartphone',title:'App Task',amount:500,date:'Today, 10:32 AM',status:'Completed'},{id:'t2',icon:'clipboard-list',title:'Survey Reward',amount:750,date:'Today, 10:15 AM',status:'Completed'},{id:'t3',icon:'gamepad-2',title:'Game Challenge',amount:200,date:'Today, 9:48 AM',status:'Completed'},{id:'t4',icon:'users-round',title:'Referral bonus',amount:450,date:'Yesterday, 6:20 PM',status:'Completed'},{id:'t5',icon:'users-round',title:'Referral bonus',amount:300,date:'Yesterday, 4:06 PM',status:'Completed'},{id:'t6',icon:'wallet-cards',title:'Withdrawal',amount:-10000,date:'Aug 22, 2:12 PM',status:'Pending'}],surveyCompleted:false,gameCompleted:false,referrals:[...Array(24)].map((_,i)=>({name:['Sarah','Michael','John','Ava','Noah'][i%5],status:i===2?'Pending':'Active',amount:i===2?0:(i%2?300:450)})),notifications:[{text:'You earned 500 Coins',time:'Today, 10:32 AM'},{text:'Survey completed',time:'Today, 10:15 AM'},{text:'New referral activity',time:'Yesterday, 6:20 PM'}],withdrawals:[]};
const KEY='earnflowDemoState'; let state=loadState(); let currentFilter='all'; let userName='Demo User';
function loadState(){try{return {...DEMO,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...DEMO}}} function save(){localStorage.setItem(KEY,JSON.stringify(state))} function money(n){return n.toLocaleString('en-US')} function completedCount(){return state.completedTasks.length} function icon(name){return `<i data-lucide="${name}"></i>`} function renderIcons(){if(window.lucide)lucide.createIcons()}
function addToast(message,type='success'){const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;document.getElementById('toast-root').append(el);setTimeout(()=>el.remove(),3600)}
function go(page){location.hash=page} function setNav(page){document.querySelectorAll('[data-nav]').forEach(el=>el.classList.toggle('active',el.dataset.nav===page))}
function taskCard(task){const done=state.completedTasks.includes(task.id);return `<article class="task-card"><div class="task-icon">${icon(task.icon)}</div><h3>${task.title}</h3><p>${task.description}</p><div class="task-bottom"><div><div class="reward">+${money(task.reward)} Coins</div><div class="time">${icon('clock-3')} ${task.time}</div></div><button class="btn ${done?'btn-ghost':'btn-primary'}" data-task="${task.id}" ${done?'disabled':''}>${done?'✓ Completed':task.game?'Play':'Start'}</button></div></article>`}
function layout(content,page){document.getElementById('app').innerHTML=`<div class="page">${content}<footer>EarnFlow Demo<br>Rewards and withdrawals shown are simulated.<br>Built by StrongNationDev</footer></div>`;setNav(page);renderIcons()}
function home(){const pct=Math.min(100,completedCount()/5*100);layout(`<div class="hero-row"><div><p class="eyebrow">Your earning dashboard</p><h1 class="hero-title">Good morning, ${userName.split(' ')[0]}.</h1><p class="hero-subtitle">Ready to earn something extra today?</p></div><button class="btn btn-primary" data-nav="tasks">Explore tasks ${icon('arrow-up-right')}</button></div><section class="balance-card"><div class="balance-label">TOTAL BALANCE</div><div class="balance-amount" data-counter="${state.balance}">0 <span>Coins</span></div><p class="usd">≈ $${(state.balance/1000).toFixed(2)}</p><div class="today">+1,850 today</div><div class="card-actions"><button class="btn btn-light" data-action="withdraw">Withdraw</button><button class="btn btn-light" data-nav="wallet">History</button></div></section><div class="grid-2"><section class="panel"><div class="section-head"><h2 class="section-title">Today's progress</h2><span class="muted small">${completedCount()} / 5 tasks</span></div><div class="progress-track"><div class="progress-fill" style="width:${pct}%"></div></div><div class="progress-meta"><span class="muted">Keep your streak going</span><strong>${Math.round(pct)}%</strong></div></section><section class="referral-promo"><h3>Invite & earn</h3><p>Earn 10% from eligible referral activity. Grow your rewards together.</p><button class="btn" data-nav="referrals">Invite friends ${icon('arrow-right')}</button></section></div><section style="margin-top:28px"><div class="section-head"><h2 class="section-title">Today's tasks</h2><button class="btn btn-ghost" data-nav="tasks">View all</button></div><div class="task-grid">${TASKS.slice(0,3).map(taskCard).join('')}</div></section><section class="panel" style="margin-top:24px"><div class="section-head"><h2 class="section-title">Recent earnings</h2><button class="btn btn-ghost" data-nav="wallet">View all</button></div><div class="activity-list">${state.transactions.slice(0,3).map(t=>`<div class="activity"><div class="activity-icon">${icon(t.icon)}</div><div class="activity-info"><strong>${t.title}</strong><span>${t.date}</span></div><span class="reward">${t.amount>0?'+':''}${money(t.amount)}</span></div>`).join('')}</div></section>`, 'home');animateCounters()}
function tasks(){const shown=currentFilter==='all'?TASKS:TASKS.filter(t=>t.type===currentFilter);layout(`<div class="hero-row"><div><p class="eyebrow">Task marketplace</p><h1 class="hero-title">Earn more coins.</h1><p class="hero-subtitle">Choose a task and start earning.</p></div></div><div class="filters">${[['all','All'],['apps','Apps'],['surveys','Surveys'],['games','Games']].map(([id,label])=>`<button class="filter ${currentFilter===id?'active':''}" data-filter="${id}">${label}</button>`).join('')}</div><div class="task-grid full-grid">${shown.map(taskCard).join('')}</div>`, 'tasks')}
function referrals(){layout(`<div class="hero-row"><div><p class="eyebrow">Referral center</p><h1 class="hero-title">Invite friends.<br>Earn more.</h1><p class="hero-subtitle">Share the upside with your community.</p></div></div><section class="referral-promo"><h3>10% Referral Bonus</h3><p>Eligible activity from your referrals adds more coins to your journey.</p></section><div class="stats-grid"><div class="stat"><strong>24</strong><span>Total referrals</span></div><div class="stat"><strong>18</strong><span>Active</span></div><div class="stat"><strong>2,450</strong><span>Referral earnings</span></div></div><section class="panel"><div class="section-head"><h2 class="section-title">Your referral link</h2></div><div class="link-box"><code>https://t.me/EarnFlowDemoBot?start=DEMO123</code><button class="icon-button" data-action="copy" aria-label="Copy referral link">${icon('copy')}</button></div><div class="modal-actions"><button class="btn btn-primary" data-action="share">${icon('send')} Share link</button></div></section><section class="panel" style="margin-top:18px"><div class="section-head"><h2 class="section-title">Referral activity</h2></div><div class="activity-list">${state.referrals.slice(0,6).map(r=>`<div class="referral-row"><div class="avatar">${r.name.slice(0,2).toUpperCase()}</div><div class="activity-info"><strong>${r.name}</strong><span class="status ${r.status.toLowerCase()}">${r.status}</span></div><span class="reward">+${r.amount} Coins</span></div>`).join('')}</div></section>`, 'referrals')}
function wallet(){layout(`<div class="hero-row"><div><p class="eyebrow">Your wallet</p><h1 class="hero-title">Money, in motion.</h1><p class="hero-subtitle">Track rewards and demo withdrawals in one place.</p></div></div><section class="balance-card"><div class="balance-label">AVAILABLE BALANCE</div><div class="balance-amount">${money(state.balance)} <span>Coins</span></div><p class="usd">≈ $${(state.balance/1000).toFixed(2)}</p><div class="today">Pending <strong>1,200</strong> · Total earned <strong>27,850</strong></div><div class="card-actions"><button class="btn btn-light" data-action="withdraw">Withdraw</button></div></section><section class="panel" style="margin-top:24px"><div class="section-head"><h2 class="section-title">Transaction history</h2></div>${state.transactions.length?state.transactions.map(t=>`<div class="transaction"><div class="activity-icon">${icon(t.icon)}</div><div class="activity-info"><strong>${t.title}</strong><span>${t.date} · <span class="status ${t.status.toLowerCase()}">${t.status}</span></span></div><span class="amount ${t.amount>0?'positive':'negative'}">${t.amount>0?'+':''}${money(t.amount)}</span></div>`).join(''):`<div class="empty">${icon('receipt')}<br><strong>No transactions yet</strong><br>Complete your first task to start earning.</div>`}</section>`, 'wallet')}
function profile(){layout(`<section class="panel" style="margin-top:20px"><div class="profile-head"><div class="avatar">DU</div><div><h1 class="section-title">${userName}</h1><p class="muted small">Member since August 2026</p></div></div><div class="stats-grid"><div class="stat"><strong>${money(state.balance)}</strong><span>Balance</span></div><div class="stat"><strong>14</strong><span>Completed tasks</span></div><div class="stat"><strong>24</strong><span>Referrals</span></div></div><div class="menu-list">${[['bell','Notifications','notifications'],['circle-help','Help Center','info'],['file-text','Terms','info'],['shield-check','Privacy','info'],['info','About EarnFlow','info']].map(([i,l,a])=>`<button class="menu-item" data-action="${a}" data-info="${l}">${icon(i)}<span>${l}</span>${icon('chevron-right')}</button>`).join('')}</div></section>`, 'profile')}
function animateCounters(){document.querySelectorAll('[data-counter]').forEach(el=>{const end=Number(el.dataset.counter);let value=0;const tick=()=>{value=Math.min(end,value+Math.ceil(end/24));el.innerHTML=`${money(value)} <span>Coins</span>`;if(value<end)requestAnimationFrame(tick)};setTimeout(tick,180)})}
function openModal(html){document.getElementById('modal-root').innerHTML=`<div class="modal-backdrop" data-close="true"><section class="modal" role="dialog" aria-modal="true">${html}</section></div>`;renderIcons()}
function closeModal(){document.getElementById('modal-root').innerHTML=''}
function taskDetail(id){const task=TASKS.find(t=>t.id===id);if(state.completedTasks.includes(id))return addToast('This task is already completed','');openModal(`<button class="modal-close" data-close="true" aria-label="Close">${icon('x')}</button><div class="task-icon">${icon(task.icon)}</div><h2>${task.title}</h2><p class="modal-sub">${task.description}</p><div class="reward" style="font-size:20px">+${money(task.reward)} Coins</div><div class="step-list"><div class="step"><span class="step-num">1</span>Start the demo task.</div><div class="step"><span class="step-num">2</span>Complete the required action.</div><div class="step"><span class="step-num">3</span>Return to EarnFlow and verify.</div></div><p class="muted small">Estimated time · ${task.time} &nbsp; · &nbsp; Fictional demo task</p><div class="modal-actions"><button class="btn btn-primary" data-start-task="${id}">${icon('play')} Start demo task</button></div>`)}
function completeTask(id){const task=TASKS.find(t=>t.id===id);closeModal();openModal(`<div class="success-view"><div class="task-icon" style="margin:auto">${icon(task.icon)}</div><h2>Preparing task...</h2><p class="modal-sub">Connecting to the demo experience</p><div class="progress-track"><div class="progress-fill" style="width:35%"></div></div></div>`);setTimeout(()=>{document.querySelector('.success-view').innerHTML=`<div class="success-view"><div class="task-icon" style="margin:auto">${icon('loader-circle')}</div><h2>Verifying completion...</h2><p class="modal-sub">Checking your demo activity</p></div>`;renderIcons()},900);setTimeout(()=>{state.completedTasks.push(id);state.balance+=task.reward;state.transactions.unshift({id:'t'+Date.now(),icon:task.icon,title:task.title,amount:task.reward,date:'Just now',status:'Completed'});save();openModal(`<div class="success-view"><div class="success-mark">${icon('check')}</div><h2>Task completed!</h2><p class="modal-sub">Your reward has been added to the demo wallet.</p><div class="success-amount">+${money(task.reward)} Coins</div><div class="modal-actions"><button class="btn btn-primary" data-close="true">Continue earning</button></div></div>`);addToast(`+${money(task.reward)} Coins added`);},1800)}
function survey(id){let q=0,answers=[];const questions=[['How often do you use mobile applications?',['Daily','Several times a week','Occasionally','Rarely']],['Which category do you use most?',['Entertainment','Shopping','Education','Finance']],['How interested are you in rewards apps?',['Very interested','Interested','Neutral','Not interested']],['How would you rate this demo?',['Excellent','Good','Fair','Needs work']]];function draw(){const [question,options]=questions[q];document.querySelector('.modal').innerHTML=`<button class="modal-close" data-close="true">${icon('x')}</button><p class="eyebrow">Question ${q+1} of 4</p><h2>${question}</h2><div class="progress-track"><div class="progress-fill" style="width:${(q+1)*25}%"></div></div><div>${options.map(o=>`<button class="survey-option ${answers[q]===o?'selected':''}" data-option="${o}">${o}</button>`).join('')}</div><div class="modal-actions"><button class="btn btn-primary" data-next-survey ${answers[q]?'':'disabled'}>${q===3?'Submit survey':'Next question'} ${icon('arrow-right')}</button></div>`;renderIcons()}openModal('<div class="modal"><p>Loading survey...</p></div>');setTimeout(()=>draw(),450);document.getElementById('modal-root').addEventListener('click',e=>{if(e.target.closest('[data-option]')){answers[q]=e.target.closest('[data-option]').dataset.option;draw()}if(e.target.closest('[data-next-survey]')){if(q<3){q++;draw()}else{state.surveyCompleted=true;completeTask(id)}}},{once:false})}
function game(id){let count=0,time=10,timer;openModal(`<button class="modal-close" data-close="true">${icon('x')}</button><p class="eyebrow">Mini game</p><h2>Tap Challenge</h2><p class="modal-sub">Tap the button 10 times before the timer ends.</p><div class="game-box"><div class="muted small">Time remaining <strong id="game-time">10s</strong></div><div class="game-count"><span id="game-count">0</span> / 10</div><button class="tap-btn" data-tap>TAP</button></div>`);timer=setInterval(()=>{time--;const t=document.getElementById('game-time');if(t)t.textContent=`${time}s`;if(time<=0){clearInterval(timer);if(count<10){document.querySelector('.game-box').innerHTML=`<div class="success-view"><div class="task-icon" style="margin:auto">${icon('timer-off')}</div><h2>Time's up</h2><p class="modal-sub">Give it another shot.</p><button class="btn btn-primary" data-retry-game>Try again</button></div>`;renderIcons()} }},1000);document.getElementById('modal-root').onclick=e=>{if(e.target.closest('[data-tap]')){count++;document.getElementById('game-count').textContent=count;if(count===10){clearInterval(timer);completeTask(id)}}if(e.target.closest('[data-retry-game]')){clearInterval(timer);closeModal();game(id)}}}
function withdrawal(){openModal(`<button class="modal-close" data-close="true">${icon('x')}</button><h2>Withdraw earnings</h2><p class="modal-sub">Available: <strong>${money(state.balance)} Coins</strong> · Minimum 10,000 Coins</p><form id="withdraw-form"><label class="form-label">Amount in coins</label><input class="input" name="amount" type="number" placeholder="10000" min="10000"><div class="error" data-error="amount"></div><label class="form-label">Payment method</label><div class="method-grid">${[['upi','UPI'],['coins','USDT'],['building-2','Bank transfer']].map(([i,l])=>`<button type="button" class="method" data-method="${l}">${icon(i)}${l}</button>`).join('')}</div><div class="error" data-error="method"></div><label class="form-label">Payment details</label><input class="input" name="details" placeholder="Enter demo payment details"><div class="error" data-error="details"></div><button class="btn btn-primary" style="width:100%;margin-top:20px" type="submit">${icon('send')} Request withdrawal</button></form>`);document.getElementById('modal-root').onclick=e=>{const m=e.target.closest('[data-method]');if(m){document.querySelectorAll('.method').forEach(x=>x.classList.remove('selected'));m.classList.add('selected')}};document.getElementById('withdraw-form').onsubmit=e=>{e.preventDefault();const fd=new FormData(e.target),amount=Number(fd.get('amount')),method=document.querySelector('.method.selected'),details=fd.get('details').trim();let valid=true;for(const key of ['amount','method','details'])document.querySelector(`[data-error="${key}"]`).textContent='';if(!amount||amount<10000||amount>state.balance){document.querySelector('[data-error="amount"]').textContent=amount>state.balance?'Amount cannot exceed your balance.':'Enter at least 10,000 coins.';valid=false}if(!method){document.querySelector('[data-error="method"]').textContent='Choose a payment method.';valid=false}if(!details){document.querySelector('[data-error="details"]').textContent='Payment details are required.';valid=false}if(!valid)return;state.balance-=amount;state.transactions.unshift({id:'w'+Date.now(),icon:'wallet-cards',title:'Withdrawal',amount:-amount,date:'Just now',status:'Pending'});state.withdrawals.push({amount,method:method.dataset.method,reference:'WD-'+String(state.withdrawals.length+1).padStart(6,'0')});save();openModal(`<div class="success-view"><div class="success-mark">${icon('check')}</div><h2>Withdrawal request submitted</h2><p class="modal-sub">Your demo request is now in review.</p><div class="success-amount">${money(amount)} Coins</div><p class="muted small">Status: <strong>Pending</strong><br>Reference: <strong>${state.withdrawals.at(-1).reference}</strong></p><div class="modal-actions"><button class="btn btn-primary" data-close="true">Done</button></div></div>`);addToast('Withdrawal request submitted')};}
function notifications(){openModal(`<button class="modal-close" data-close="true">${icon('x')}</button><p class="eyebrow">Inbox</p><h2>Notifications</h2>${state.notifications.map(n=>`<div class="notification-item">${icon('sparkles')} ${n.text}<small>${n.time}</small></div>`).join('')}`)}
function info(title){openModal(`<button class="modal-close" data-close="true">${icon('x')}</button><p class="eyebrow">EarnFlow demo</p><h2>${title}</h2><p class="modal-sub">This is a product demonstration. Content, rewards, and account details are fictional and stored only in this browser.</p><button class="btn btn-primary" data-close="true">Close</button>`)}
function initTelegram(){const tg=window.Telegram?.WebApp;if(tg){tg.ready();tg.expand();const user=tg.initDataUnsafe?.user;if(user?.first_name)userName=user.first_name;if(user?.id)document.querySelector('.top-avatar').textContent=String(user.id).slice(-2)}}
function render(){const page=location.hash.slice(1)||'home';({home,tasks,referrals,wallet,profile}[page]||home)()}
document.addEventListener('click',e=>{const nav=e.target.closest('[data-nav]');if(nav){e.preventDefault();go(nav.dataset.nav);return}const close=e.target.closest('[data-close]');if(close){closeModal();return}const task=e.target.closest('[data-task]');if(task){const item=TASKS.find(t=>t.id===task.dataset.task);item.survey?survey(item.id):item.game?game(item.id):taskDetail(item.id);return}const start=e.target.closest('[data-start-task]');if(start){completeTask(start.dataset.startTask);return}const filter=e.target.closest('[data-filter]');if(filter){currentFilter=filter.dataset.filter;tasks();return}const action=e.target.closest('[data-action]');if(action){const a=action.dataset.action;if(a==='notifications')notifications();if(a==='withdraw')withdrawal();if(a==='copy'){navigator.clipboard?.writeText('https://t.me/EarnFlowDemoBot?start=DEMO123');addToast('Referral link copied!')}if(a==='share'){const url='https://t.me/EarnFlowDemoBot?start=DEMO123';if(window.Telegram?.WebApp?.openTelegramLink)Telegram.WebApp.openTelegramLink(`https://t.me/share/url?url=${encodeURIComponent(url)}`);else if(navigator.share)navigator.share({title:'EarnFlow',url}).catch(()=>{});else addToast('Referral link ready to share')}if(a==='info')info(action.dataset.info)}});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});window.addEventListener('hashchange',render);if(new URLSearchParams(location.search).get('resetDemo')==='true'){localStorage.removeItem(KEY);location.replace(location.pathname)}initTelegram();render();
